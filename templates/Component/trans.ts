import { makeTranslations } from '../../config.littera';

export const useTrans = makeTranslations({
  en_US: {
    example: `Example`,
  },
  de_DE: {
    example: `Beispiel`,
  },
  pl_PL: {
    example: `Przykład`,
  },
});
